# simple-kv-store
A simple in-memory key-value store built in Python with a CLI interface. Supports basic commands like SET, GET, and DEL, with a clean separation between storage, command parsing, and REPL layers. Designed as a foundation for a Redis-like system with future support for networking and persistence.
